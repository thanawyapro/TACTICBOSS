# تقرير إصلاح Tactic Boss V132.1

## سبب المشكلة المرجح

- خلط بين حزمة سورس وحزمة Prebuilt.
- نشر التطبيق من `dist` مع إعدادات Netlify قديمة.
- Service Worker قديم يحتفظ بـ `index.html` أو ملفات واجهة من إصدار سابق.
- استعادة شاشة قديمة من localStorage بدل فتح الصفحة الرئيسية.

## الإصلاحات المنفذة

- نشر التطبيق من جذر المستودع مباشرة (`publish = "."`).
- عدم تشغيل npm أو Build داخل Netlify.
- إضافة شاشة تحميل سليمة قبل تشغيل React.
- إعادة المستخدم إلى الصفحة الرئيسية مرة واحدة عند الانتقال إلى V132.1.
- حذف كاش Tactic Boss القديم تلقائيًا.
- إلغاء تسجيل Service Worker السابق.
- تحويل Service Worker الحالي إلى ملف تنظيف ذاتي ثم إلغاء نفسه.
- منع تخزين `index.html` وruntime config وversion في الكاش.
- الإبقاء على كاش assets ذات الأسماء المشفرة فقط.

## التحقق

- TypeScript: ناجح.
- Production Build: ناجح.
- اختبارات الإصدار الحرجة: 23/23 ناجحة.
- مراجع ملفات JavaScript وCSS في `index.html`: موجودة داخل الحزمة.
