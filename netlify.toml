[build]
  publish = "."
  functions = "netlify/functions"
  command = "echo 'Tactic Boss V132.1 flat recovery: verified prebuilt files — no npm build required'"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
