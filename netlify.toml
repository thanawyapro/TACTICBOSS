[build]
  publish = "dist"
  functions = "netlify/functions"
  command = "echo 'Tactic Boss V132: using verified prebuilt dist — no npm build required'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
