[build]
  publish = "dist"
  functions = "netlify/functions"
  command = "echo 'Tactic Boss V132.4: verified prebuilt dist with Tailwind CSS — no npm build required'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
