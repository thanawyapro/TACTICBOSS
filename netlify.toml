[build]
  publish = "dist"
  functions = "netlify/functions"
  command = "echo 'Tactic Boss V132.2: verified prebuilt dist — no npm build required'"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
