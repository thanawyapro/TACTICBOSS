{
  "version": "v105.5-github-netlify-web-clean-no-capacitor",
  "build": "web-only-deploy-removes-capacitor-cli-node-conflict",
  "requiresClearCache": true,
  "notes": "V105.5 removes Capacitor/mobile-only build tooling from the Netlify web package, keeps Vite in production dependencies, and uses a lightweight production install fallback to avoid Node/npm deploy conflicts."
}
